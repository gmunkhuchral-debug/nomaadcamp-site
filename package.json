{
  "name": "nomaadcamp-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-manifest": "node scripts/generate-manifest.js",
    "build:webp": "node scripts/build-webp.js",
    "build:webm": "bash scripts/build-webm.sh",
    "build:minify": "node scripts/build-minify.js",
    "build": "node scripts/generate-manifest.js && node scripts/build-webp.js && bash scripts/build-webm.sh && node scripts/build-minify.js"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "sharp": "^0.33.5",
    "terser": "^5.36.0"
  }
}
